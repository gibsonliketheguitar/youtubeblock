import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main className="flex-grow flex flex-col justify-center items-center bg-gray-200">
      <div className="text-2xl sm:text-6xl md:text-9xl">hello World</div>
    </main>
  );
};

export default Home;
